---
const { src, type = 'video/mp4', controls = true } = Astro.props;
---

<video {controls} style="width: 100%;">
  <source src={src} type={type} />
  Your browser does not support the video tag.
</video>